name: Go

on:
  push:
  workflow_dispatch:

jobs:
  build-1:
    runs-on: tenki-local-macos-26-mini
    steps:
      - name: Sleep and Print
        run: |
          echo "full e2e: webhook -> engine -> scheduler -> sandbox -> macOS VM"
          uname -a
          sw_vers
          id
          sleep 20
          echo 11111111
