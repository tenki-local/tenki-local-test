name: Go

on:
  push:
  workflow_dispatch:

jobs:
  build-1:
    runs-on: tenki-local-macos-26-small
    steps:
      - name: Sleep and Print
        run: |
          uname -a
          sw_vers
          id
          sleep 20
          echo 11111111
